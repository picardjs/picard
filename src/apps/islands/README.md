# Apps: Islands

This variant of Picard is an adapter that is used to extend the reach of `server`. It can be thought of as a lightweight client that can be used directly in the browser, without having to build it with some tooling first.
